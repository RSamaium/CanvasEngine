<Canvas width="100%" height="100%">
   <Child>
      @for (obj of gui) {
        @for (obj of gui) {
          <Text text={obj} color="red" />
        }
      }
   </Child>
  
</Canvas>
<script>

  import {
    animatedSignal,
    computed,
    effect,
    signal,
    trigger,
    mount
  } from "canvasengine";
  import Child  from "./child.ce";
  
  const gui = [1, 2, 3]
</script>