<script>
  import {
    signal,
    animatedSignal,
    mount,
    effect,
    animatedSequence,
    tick
  } from "canvasengine";

const text = signal("Hello");
const show = signal(false);
const array = signal([1, 2, 3]);
const click = (event, formData) => {
  console.log(formData)
}

const deleteItem = (item) => {
  array().splice(array().indexOf(item), 1);
}
</script>

<Canvas antialias={true} backgroundColor="white">
  <DOMContainer x={100} y={100}>
      <form submit={click}>
        <input name="username" type="text" value={text} />
        <button>aa</button>
        <p>{text}</p>
      </form>
  </DOMContainer>
</Canvas>
