<script>
  import {
    signal,
    animatedSignal,
    mount,
    effect,
    animatedSequence,
  } from "canvasengine";

const text = signal("Hello, world!");
const red = signal(true);

const click = () => {
  red.set(!red());
}

const click2 = () => {
  console.log("click2");
}
</script>

<Canvas antialias={true}>
    <div>
      <form submit={click2}>
        <input type="text" />
        <button click>Submit</button>
      </form>
    </div>
</Canvas>
