<script>
    import { signal, animatedSignal, mount, effect } from 'canvasengine'
    import Child from './child.ce'

    const bool = signal(true)

    setTimeout(() => {
        bool.set(false)
    }, 1000)
    
    </script>
    
  <Canvas>
     
    <Video 
      source="https://pixijs.com/assets/video.mp4" 
      play={bool} 
      @pause={() => console.log('pause')}
    />
     
  </Canvas>