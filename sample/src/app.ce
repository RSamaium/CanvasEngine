<script>
  import {
    signal,
    animatedSignal,
    mount,
    effect,
    animatedSequence,
  } from "canvasengine";

const text = signal("Hello, world!");
const red = signal(true);

const click = () => {
  red.set(!red());
}

const click2 = () => {
  console.log("click2");
}

const sound = { src: ['exp.mp3'], playing: true }


</script>

<Canvas antialias={true}>
  <Rect x={100} y={100} width={100} height={100} color="red" click={click2} />
  <Container sound>

  </Container>
</Canvas>
