<script>
  import {
    signal,
    animatedSignal,
    mount,
    effect,
    animatedSequence,
  } from "canvasengine";

const text = signal("Hello, world!");
</script>

<Canvas antialias={true}>
    <input type="text" value={text} />
</Canvas>
