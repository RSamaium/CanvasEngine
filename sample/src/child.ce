<Sprite image x y />
@for (item of array) {
    <Rect width={100} height={100} color="red" x={x} y={y} />
}

<script>
import { mount } from "canvasengine"

const {
    image,
    x,
    y
} = defineProps()

const array = [1, 2, 3]

mount((element) => {
    console.log(element)
})
</script>