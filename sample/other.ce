<script>
import { signal } from 'canvasengine'

const x = signal(0)
const y = signal(0)
const { value} = $props

const controls = signal({})
const list = signal([100,200,300])

function draw(g) {
    g.rect(0, 0, 100, 100)
    g.fill('white')
}
</script>

@for (value of list) {
   <Graphics @y={value} @draw />
}
