<script>
    import Ambiant from "./ambiant.ce";
    import Flash from "./flash.ce";
    import Recta from "./rect.ce";
    import { trigger } from 'canvasengine';

    const flash = trigger()

    const click = () => {
        flash.start()
    }

    const dragStart = () => {
        console.log('dragStart')
    }
</script>

<Canvas 
    backgroundColor="white" 
    width="100%" 
    height="100%"
    antialias="true"
>
    <Viewport  
        worldHeight="1000" 
        worldWidth="1000" 
        clamp="all" 
        dragg={true} 
        wheel={{}} 
        decelerate={{}} 
        pinch={{}} 
        snap={{}}
        @dragStart={dragStart}
    >
        <Recta  />
    </Viewport>
</Canvas>