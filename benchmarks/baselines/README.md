# Benchmark Baselines

Put official internal baselines here once a stable local or CI environment is
chosen.

Suggested files:

- `reactivity.json`
- `render.json`

Do not compare render benchmarks across unrelated machines unless the
environment fingerprint is intentionally ignored during analysis.
